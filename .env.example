ADMIN_PASSWORD=change-this-password
ADMIN_SECRET=change-this-secret

# Required for homepage admin saves on Vercel/serverless deployments.
# The JSON file remains the local fallback when these are omitted.
BLOB_STORE_ID=
BLOB_READ_WRITE_TOKEN=
